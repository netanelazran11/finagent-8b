# --- Real market data (free tier: 25 req/day) ---
# https://www.alphavantage.co/support/#api-key
ALPHAVANTAGE_API_KEY=

# --- LLM-as-judge for the eval harness (scripts/eval.py) ---
OPENAI_API_KEY=

# --- Synthetic dataset generation (scripts/generate_dataset.py) ---
# Same OPENAI_API_KEY is reused unless you want a separate budget.

# --- Optional: push fine-tuned model to the HF Hub ---
HUGGINGFACE_TOKEN=

# --- Optional: experiment tracking ---
WANDB_API_KEY=
